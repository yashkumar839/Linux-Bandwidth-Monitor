# Linux-Bandwidth-Monitor
It will track live bandwidth of your system.
Firstly, create a folder named "bandwidth-monitor" in your HOME folder.
Save the monitor.sh script in that folder.
Create another folder named "log" inside the bandwidth-monitor and create a file named "bandwidth.log" in it.
Now run your script in the terminal.
